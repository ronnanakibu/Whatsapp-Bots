# Changelog v5.14.12

## Bug Fixes
- **Authenticated Asset Fetching**: Added the Hugging Face Authorization header (Bearer token) to HTTP `fetch` requests inside `generateGradioSpaceImage` (image generation) and `generateVideoFromImage` (video generation) to allow the bot to successfully download generated files from private Spaces without getting blocked/redirected to login page HTML.
