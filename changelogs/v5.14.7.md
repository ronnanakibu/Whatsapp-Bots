# Changelog v5.14.7

## Bug Fixes
- **Fix Manual Music Upload Network Error**: Resolved a network error on the dashboard's manual music upload page when deployed to standalone hosting environments (such as Vercel). Replaced hardcoded localhost port-swap logic with the dynamic API host resolver, ensuring that all API queries (upload audio, configuration fetch, Suno cookie sync, and tunnel proxy update) route dynamically to the correct bot server.
